#include "Terminate_0Lepton.h"

void Terminate_0Lepton()
{
    //==============================================
    // Terminate_1Lep4jet:
    // This function gets called after the event looping.
    // This is where one does stuff that needs to happen after the event looping for category 1Lep4jet.
    //==============================================

    // To place eventlist writing or something that needs to happen post loop.
    // TODO: implement event list writing
}
